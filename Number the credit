#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int x , y ,z;
    cin >> x >> y >> z;
    cout<<(x*4) + (y*2)<<endl;
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t = 1;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}  
